If you need to use a program which is not installed in the system you can restor to using a `nix shell`.

If you need to reference some third party code which is not available on the machine prefer cloning the repository to a `/tmp/opencode` rather than guessing Github URLs and APIs.
